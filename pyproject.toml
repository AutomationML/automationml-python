[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "automationml"
version = "0.1.0a1"
description = "JSON-first Python SDK for AutomationML CAEX 3.0 documents."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [
  { name = "AutomationML SDK Contributors" }
]
maintainers = [
  { name = "AutomationML Association" }
]
keywords = ["automationml", "aml", "caex", "pydantic", "json", "xml"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "Intended Audience :: Manufacturing",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: File Formats",
  "Topic :: Software Development :: Libraries"
]
dependencies = [
  "pydantic>=2.7,<3"
]

[project.optional-dependencies]
xml = [
  "pydantic-xml>=2.12,<3"
]
dev = [
  "jsonschema>=4.22",
  "pytest>=8.2",
  "xmlschema>=3.4"
]

[project.urls]
Homepage = "https://www.automationml.org/"
Source = "https://github.com/AutomationML/automationml-python"
Documentation = "https://github.com/AutomationML/automationml-python/tree/main/docs"
Issues = "https://github.com/AutomationML/automationml-python/issues"
Changelog = "https://github.com/AutomationML/automationml-python/blob/main/CHANGELOG.md"

[tool.hatch.build.targets.wheel]
packages = ["src/automationml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
